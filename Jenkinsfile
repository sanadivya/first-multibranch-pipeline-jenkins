pipeline {
  agent any
  stages {
    stage('Build code') {
      steps {
        bat 'echo Building from develop branch'
      }
    }
    stage('Deploy code') {
      steps {
        bat 'echo Deploying from develop branch'
      }
    }
  }
}
